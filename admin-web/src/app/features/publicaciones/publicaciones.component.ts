import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../../core/services/admin-api.service';import { PublicacionResponse } from '../../core/models/api.models';import { StatusBadgeComponent } from '../../shared/components/status-badge.component';
@Component({standalone:true,imports:[CommonModule,FormsModule,StatusBadgeComponent],template:`<h2>Publicaciones</h2><div class="row mb-3"><div class="col"><input class="form-control" placeholder="Distrito" [(ngModel)]="distrito"></div><div class="col"><select class="form-select" [(ngModel)]="estado"><option value="">Todos</option><option>DISPONIBLE</option><option>RESERVADA</option><option>ENTREGADA</option><option>VENCIDA</option><option>BLOQUEADA</option></select></div></div><div class="card table-card"><table class="table"><tr><th>Alimento</th><th>Categoría</th><th>Cantidad</th><th>Distrito</th><th>Estado</th><th></th></tr><tr *ngFor="let p of filtradas()"><td>{{p.nombreAlimento}}</td><td>{{p.categoria}}</td><td>{{p.cantidadDisponible}} {{p.unidadMedida}}</td><td>{{p.distrito}}</td><td><app-status-badge [value]="p.estado" /></td><td><button class="btn btn-sm btn-success" (click)="aprobar(p)">Aprobar</button> <button class="btn btn-sm btn-danger" (click)="bloquear(p)">Bloquear</button></td></tr></table></div>`})
export class PublicacionesComponent implements OnInit{items:PublicacionResponse[]=[];estado='';distrito='';constructor(private api:AdminApiService){}ngOnInit(){this.load()}load(){this.api.publicaciones().subscribe(r=>this.items=r.data)}filtradas(){return this.items.filter(p=>(!this.estado||p.estado===this.estado)&&(!this.distrito||p.distrito?.toLowerCase().includes(this.distrito.toLowerCase())))}aprobar(p:PublicacionResponse){this.api.aprobarPublicacion(p.idPublicacion).subscribe(()=>this.load())}bloquear(p:PublicacionResponse){this.api.bloquearPublicacion(p.idPublicacion).subscribe(()=>this.load())}}
