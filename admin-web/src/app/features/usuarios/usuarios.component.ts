import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../../core/services/admin-api.service';import { UsuarioResponse } from '../../core/models/api.models';import { StatusBadgeComponent } from '../../shared/components/status-badge.component';
@Component({standalone:true,imports:[CommonModule,FormsModule,StatusBadgeComponent],template:`<h2>Usuarios</h2><div class="row mb-3"><div class="col"><select class="form-select" [(ngModel)]="rol" (change)="load()"><option value="">Todos</option><option>DONANTE</option><option>RECEPTOR</option><option>ADMIN</option><option>ADMINISTRADOR</option></select></div><div class="col"><select class="form-select" [(ngModel)]="estado"><option value="">Todos estados</option><option>ACTIVO</option><option>INACTIVO</option><option>BLOQUEADO</option></select></div></div><div class="card table-card"><table class="table"><tr><th>Usuario</th><th>Correo</th><th>Rol</th><th>Estado</th><th></th></tr><tr *ngFor="let u of filtrados()"><td>{{u.nombres}} {{u.apellidos}}</td><td>{{u.correo}}</td><td>{{u.rol}}</td><td><app-status-badge [value]="u.estado" /></td><td><button class="btn btn-sm btn-danger" (click)="estadoUsuario(u,'BLOQUEADO')">Bloquear</button> <button class="btn btn-sm btn-success" (click)="estadoUsuario(u,'ACTIVO')">Activar</button></td></tr></table></div>`})
export class UsuariosComponent implements OnInit{items:UsuarioResponse[]=[];rol='';estado='';constructor(private api:AdminApiService){}ngOnInit(){this.load()}load(){(this.rol?this.api.usuarioPorRol(this.rol):this.api.usuarios()).subscribe(r=>this.items=r.data)}filtrados(){return this.items.filter(u=>!this.estado||u.estado===this.estado)}estadoUsuario(u:UsuarioResponse,e:string){this.api.cambiarEstadoUsuario(u.idUsuario,e).subscribe(()=>this.load())}}
