import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { Router } from '@angular/router';import { AuthService } from '../../core/services/auth.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class="min-vh-100 d-flex align-items-center justify-content-center page"><form class="card stat-card p-4" style="width:380px" [formGroup]="form" (ngSubmit)="login()"><h3 class="mb-3 text-success">Admin Login</h3><input class="form-control mb-3" placeholder="Correo" formControlName="correo"><input class="form-control mb-3" placeholder="Contraseña" type="password" formControlName="password"><button class="btn btn-success w-100" [disabled]="form.invalid||loading">Ingresar</button><div class="text-danger mt-3" *ngIf="error">{{error}}</div></form></div>`})
export class LoginComponent{loading=false;error='';form=this.fb.group({correo:['',[Validators.required,Validators.email]],password:['',Validators.required]});constructor(private fb:FormBuilder,private auth:AuthService,private router:Router){} login(){if(this.form.invalid)return;this.loading=true;this.auth.login(this.form.getRawValue() as any).subscribe({next:()=>this.router.navigateByUrl('/dashboard'),error:e=>{this.error=e.message||'No tienes permisos';this.loading=false}})}}
