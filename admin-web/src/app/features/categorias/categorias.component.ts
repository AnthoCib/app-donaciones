import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';import { AdminApiService } from '../../core/services/admin-api.service';import { CategoriaResponse } from '../../core/models/api.models';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<h2>Categorías</h2><form class="d-flex gap-2 mb-3" [formGroup]="form" (ngSubmit)="save()"><input class="form-control" formControlName="nombre" placeholder="Nombre categoría"><button class="btn btn-success">Guardar</button></form><div class="card table-card"><table class="table"><tr><th>Nombre</th><th>Estado</th><th></th></tr><tr *ngFor="let c of items"><td>{{c.nombre}}</td><td>{{c.estado?'Activa':'Inactiva'}}</td><td><button class="btn btn-sm btn-outline-success" (click)="toggle(c)">{{c.estado?'Desactivar':'Activar'}}</button></td></tr></table></div>`})
export class CategoriasComponent implements OnInit{items:CategoriaResponse[]=[];form=this.fb.group({nombre:['',Validators.required]});constructor(private api:AdminApiService,private fb:FormBuilder){}ngOnInit(){this.load()}load(){this.api.categorias().subscribe(r=>this.items=r.data)}save(){if(this.form.invalid)return;this.api.crearCategoria(this.form.getRawValue() as any).subscribe(()=>{this.form.reset();this.load()})}toggle(c:CategoriaResponse){this.api.estadoCategoria(c.idCategoria,!c.estado).subscribe(()=>this.load())}}
